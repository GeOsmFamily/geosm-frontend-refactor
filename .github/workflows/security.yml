name: Sécurité

on:
  push:
    branches: [main, dev, 'claude/**']
  pull_request:
    branches: [main, dev]
  schedule:
    - cron: '0 6 * * 1'

permissions:
  contents: read

jobs:
  audit:
    name: Audit des dépendances
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
      - run: npm ci
      - name: Audit npm
        run: npm audit --audit-level=high || true
      - name: Rapport d'audit détaillé
        run: npm audit --json > npm-audit-report.json || true
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: npm-audit-report
          path: npm-audit-report.json
          retention-days: 30

  secrets-scan:
    name: Détection de secrets
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Installer Gitleaks
        run: |
          curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.21.2/gitleaks_8.21.2_linux_x64.tar.gz | tar xz
          sudo mv gitleaks /usr/local/bin/
      - name: Scan des secrets
        run: gitleaks detect --source . --report-format json --report-path gitleaks-report.json --verbose || true
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: gitleaks-report
          path: gitleaks-report.json
          retention-days: 30

  docker-security:
    name: Scan image Docker
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v4
      - name: Build image Docker
        uses: docker/build-push-action@v7
        with:
          context: .
          push: false
          load: true
          tags: geosm-frontend:security-scan
      - name: Scan Trivy
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: 'geosm-frontend:security-scan'
          format: 'table'
          severity: 'CRITICAL'
          # exit-code: '0' faisait que ce job ne pouvait jamais échouer, quel que soit le
          # nombre de vulnérabilités - voir le même correctif côté backend (security.yml).
          exit-code: '1'
